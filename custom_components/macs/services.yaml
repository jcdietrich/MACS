set_mood:
  name: Set mood
  description: Set Macs mood (updates select.macs_mood).
  fields:
    mood:
      name: Mood
      description: One of bored, confused, happy, idle, listening, sleeping, surprised, thinking
      required: true
      selector:
        select:
          mode: dropdown
          options:
            - bored
            - confused
            - happy
            - idle
            - listening
            - sleeping
            - surprised
            - thinking

set_brightness:
  name: Set brightness
  description: Set MACS brightness (0–100).
  fields:
    brightness:
      name: Brightness
      description: 0 (dim) to 100 (bright)
      required: true
      selector:
        number:
          min: 0
          max: 100
          step: 1
          mode: slider
          unit_of_measurement: "%"

set_temperature:
  name: Set temperature intensity
  description: Set MACS temperature intensity (0-100).
  fields:
    temperature:
      name: Temperature
      description: 0 (none) to 100 (max)
      required: true
      selector:
        number:
          min: 0
          max: 100
          step: 1
          mode: slider
          unit_of_measurement: "%"

set_windspeed:
  name: Set wind speed intensity
  description: Set MACS wind speed intensity (0-100).
  fields:
    windspeed:
      name: Wind speed
      description: 0 (none) to 100 (max)
      required: true
      selector:
        number:
          min: 0
          max: 100
          step: 1
          mode: slider
          unit_of_measurement: "%"

set_rainfall:
  name: Set rainfall intensity
  description: Set MACS rainfall intensity (0-100).
  fields:
    rainfall:
      name: Rainfall
      description: 0 (none) to 100 (max)
      required: true
      selector:
        number:
          min: 0
          max: 100
          step: 1
          mode: slider
          unit_of_measurement: "%"

set_snowing:
  name: Set snowing
  description: Toggle MACS snow effect on/off.
  fields:
    snowing:
      name: Snowing
      description: true to enable snow effect, false to disable
      required: true
      selector:
        boolean: